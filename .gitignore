# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# 编辑器
.vscode/
.idea/
*.swp

# 本机预览配置（含绝对路径，不可移植）
.claude/

# ── 自摄照片：收件箱不入库，成品目录才入库 ──────────────────────────
# 用户往 img/inbox/ 丢原片，处理完的成品出现在 img/own/。
# **整个收件箱不入版本库**：里面有没看过的图、带人脸的图、裁前原图、
# agent 的中间产物。按目录挡比按文件名挡干净——文件名模式挡不住
# 相机不叫那个名的（20240104_140825.jpg 就漏过了），也挡不住
# agent 顺手写出来的提亮图（一次 7.5 MB 差点推上 GitHub）。
img/inbox/*
img/processing/*
!img/inbox/README.md

# ── 四夹制（用户 2026-08-21 定）：只有被用的才需要 push ──────────────
# inbox（放料）→ processing（案头）→ own（成品库，**本地**）→ used（上线层，入库）。
# img/own/ 是成品库不是上线层：出品都落这儿，但库存 200 多张、近 40 MB，
# 其中真被站点引用的只有二十来张。**成品≠上站**——被 js/pics-own-cards.js
# 点名、或被某条故事线接线点将，那一步才 git mv 进 img/used/ 并入库推送。
# 照片文件本身一张都不删，只是留在本机备用。
img/own/*
!img/own/README.md

# ── 馆方 CC0 开放图第三通道（2026-08-25 立，军规见 docs/METHODS.md）───────
# stock/ 是馆方原档备料库（vendor 落本地，动辄两三 MB），照 own/ 的规矩留在本机；
# 圈中挂卡的出 1600px 网版落 img/museum-cc0/ 顶层，只有网版入库推送。
img/museum-cc0/stock/

# Python 缓存
__pycache__/
*.pyc

# WIP 工作档本地专用（用户 2026-08-20 定）：候选暂存、圈选单、点子与计划稿不上库。
# 军规/台账/line-*.md 资料页/sources与geo构建输入 照旧入库。
docs/desk/
docs/holding/
docs/idea-*.md
docs/plan-*.md
docs/video-brief*.md

# img/originals/ 出品原片留档（2026-08-26「出品留原片」新规）：凡进 own/ 或 used/
# 的照片，原片以 <成品名>-orig.jpg 永存于此，EXIF 不剥——换 spec 重做的本钱。
# 本地永不入库推送（原片含 GPS 等隐私字段）。
img/originals/
