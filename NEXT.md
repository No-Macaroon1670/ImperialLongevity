# 待做:把事件放进河里(移动端与竖河)

用户提出、尚未实现的设计。记在这里以免散失,细节留待动手时再定。

## 核心想法

事件不再摆在河的两侧边栏,而是**尽量放进河道本身**——「事件本就发生在
历史这条河里」。有空间时直接写名字,没空间时只画标记。

## 归属:过渡事件放在过渡的正中

先前担心的是归属会变成**主张**:淝水之战放进前秦的河道还是东晋的?
放哪边都是在替读者判断。

用户的解法把这个问题解掉了:**改朝换代类的事件就放在过渡段的正中央**。
它们本来就不属于任何一边,而属于「一边变成另一边」的那一刻。

  · 河流视图已有现成的几何:buildTransitions 算出的 trans / flows / necks
    就知道每一次改道的中点在哪,直接可用。
  · 其余事件放进当时所属政权的河道。多数无歧义(安史之乱→唐);
    少数要人来定,加一个可选的 `d` 字段(政权 key)——era 类早就是这么做的,
    正是因为按重叠面积猜会把咸平之治猜到辽国、乾淳之治猜到大理。

## 密度

按分量分级控制:一等锚点全程八十余个,九股并流的年代同屏也就两三个,
不会糊成一片。二三等在河道宽松时才放出来。

## 交互

  单击 → 显示名字
  双击 → 打开知识卡

移动端另有一处**现存缺陷**:手机上根本弹不出知识卡——整个知识面板
被 `min-width: 1100px` 挡住了。需要做成手机版的单卡:一次只开一张,
开新的即关掉旧的。

## 顺序

用户:「Let's work out details when other work is finished.」
